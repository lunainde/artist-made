<div class="confirmation">
    <%= image_tag("home-banner-art.png", style: "background-size: cover; backdrop-filter: blur(6px); -webkit-filter: blur(6px); width: 100%; height: 100vh;") %>
      <div class="summary">
        <% @paid_shopping_carts.each do |paid_shopping_cart| %>
          <div class="container">
            <div class="row py-4">
             
              <div class="col" style="margin-left:20px; width:100%;">
                <h3>Checkout summary</h3>
                <div class="orders">
                <p>You have ordered: </p>
                <strong><% paid_shopping_cart.shopping_cart_items.each do |item| %>
                <p> <%= item.art_item.art.title %> - <%= item.art_item.format %> </p>
                <% end %>
                </strong> for <strong><%= paid_shopping_cart.total %> €</strong></p>
                </div>
                <h5>Thank you for your purchase! </h5>
              </div> 
              <div class="col-md-3">
                <%= image_tag paid_shopping_cart.shopping_cart_items.first.art_item.art.img_url, width: '100%' %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
</div>